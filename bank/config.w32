// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("bank", "for bank support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("bank", "enable bank support", "no");

if (PHP_BANK != "no") {
	EXTENSION("bank", "bank.c");
}

